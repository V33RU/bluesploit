# ═══════════════════════════════════════════════════════════════════════
# BlueSploit, Bluetooth Exploitation Framework
# ═══════════════════════════════════════════════════════════════════════
#
# Install:
#   pip install -r requirements.txt
#
# System packages (Debian/Ubuntu):
#   sudo apt install bluez bluetooth libbluetooth-dev python3-dev \
#                    libglib2.0-dev libboost-python-dev libboost-thread-dev
#
# Hardware-specific system packages:
#   Ubertooth One  → sudo apt install ubertooth wireshark
#   HackRF One     → sudo apt install hackrf gr-bluetooth
#   nRF52840       → flash nRF Sniffer firmware, install Wireshark plugin
#   BTLEJack       → flash BTLEJack firmware to micro:bit
#   YARD Stick One → pip install git+https://github.com/atlas0fd00m/rfcat.git
#
# macOS embedded-C / Objective-C exploit modules (modules/dos/macos_*):
#   Require Xcode Command Line Tools (clang + IOKit / Foundation headers):
#       xcode-select --install
#   install.sh checks for this automatically on Darwin.
# ═══════════════════════════════════════════════════════════════════════

# NOTE: versions pinned with == to match pyproject.toml. Source of truth
# is pyproject.toml; bump there first, then mirror here.

# Core BLE
bleak==3.0.1                   # BLE scanning + GATT (cross-platform)

# Bluetooth Classic (BR/EDR)
# pybluez2 0.46 sdist on PyPI is broken (missing btmodule.h header).
# Run ./install.sh --classic, it installs from the maintained upstream
# fork or falls back to apt python3-bluez.
# Manual fix:
#   sudo apt install libbluetooth-dev bluez build-essential
#   pip install git+https://github.com/pybluez/pybluez.git#egg=pybluez

# Packet Crafting & Crypto
scapy==2.7.0                   # BT/BLE frame construction + injection
cryptography==47.0.0           # Key derivation (E0/E3), AES analysis

# BLE Low-Level
# Modules: sweyntooth, ble_pairing_downgrade, ble_sc_bypass
bluepy==1.3.0                  # Raw BLE + GATT handle access (Linux)

# Hardware Dongles
pyserial==3.5                  # nRF Sniffer, BTLEJack, YARD Stick One
btlejack==2.1.1                # BLE connection hijacking (micro:bit)

# UI (declared for future use; not yet imported by the codebase)
rich==15.0.0                   # Colour tables, progress bars
cmd2==3.5.1                    # Advanced REPL (history, scripting)

# Async Utilities (declared for future use; not yet imported)
asyncio-dgram==3.0.0           # Async UDP/datagram support

# Development (uncomment for dev/CI, or use pip install ".[dev]")
# pytest==8.3.4
# pytest-asyncio==0.24.0
# pytest-cov==6.0.0
# pytest-mock==3.14.0
# black==24.10.0
# ruff==0.15.12
# mypy==1.13.0
