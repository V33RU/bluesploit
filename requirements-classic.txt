# BlueSploit — Bluetooth Classic dependencies
# Enables: bluefrag, knob, bias, badchoice, badkarma, blueborne_*,
#          braktooth_esp32, bluebugging, bluesnarfing, bluffs,
#          bnep_heap_disclosure, unauth_write, keystroke_injection
#
# System prerequisites (run first):
#   sudo apt install bluez bluetooth libbluetooth-dev python3-dev
#
# Then:
#   pip install -r requirements-classic.txt

-r requirements.txt

# Bluetooth Classic socket support (L2CAP, RFCOMM, HCI raw)
pybluez2>=0.46             # Maintained PyBluez fork with Python 3.10+ fixes

# Packet crafting for KNOB, BadKarma, BadChoice, BLUFFS payloads
scapy>=2.5.0

# Key derivation / cryptographic analysis (KNOB, BLUFFS)
cryptography>=41.0.0
